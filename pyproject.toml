[project]
name = "agrieco-connect-backend"
version = "0.1.0"
description = "Backend for Agrieco Connect"
requires-python = ">=3.10"

dependencies = [
    "flask",
]

[project.scripts]
app = "api.index:app"